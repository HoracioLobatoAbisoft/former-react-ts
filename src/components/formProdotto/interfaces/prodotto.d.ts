export interface InitialValuesProdotto {
  base: null | number;
  depth: null | number;
  height: null | number;
  quantity: null | number;
  Format:string
}

export interface OptionsSelect{
  label:string
  value:string | number
}
